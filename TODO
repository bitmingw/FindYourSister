Update C++ image interface.
    Use parse values as double rather than int.

Rewrite gen_labels.py
    `gen_labels.py` is a all-in-one script to generate final image labels.
    Need to add a json to config its bahavior.

Update folder values.
    Save the absolute path of file.

Porting of python2 to python3.
    Use future print function and unicode literals.

